uniform projection[4];
uniform foo;

in inpos;
in inclr;

out outpos: position;
out outclr: color;

fn main() {
    let foo = 1. * 2. + 3.
//     let inpos = [inpos.x, inpos.y, inpos.z, 1.];

//     outpos = projection * inpos;
//     outclr = inclr;
}
